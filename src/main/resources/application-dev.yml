#spring.application.name=onepage
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/onepage_db"
    username: "root"
    password: "mysql"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
